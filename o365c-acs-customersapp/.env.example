# Azure AD Configuration for MSAL Authentication
# Copy this file to .env and fill in your Azure AD app registration details

# Required: Your Azure AD Application (Client) ID
REACT_APP_CLIENT_ID=your-azure-ad-client-id-here

# Optional: Authority URL (defaults to common if not provided)
# For single tenant: https://login.microsoftonline.com/{tenant-id}
# For multi-tenant: https://login.microsoftonline.com/common
REACT_APP_AUTHORITY=https://login.microsoftonline.com/your-tenant-id-here

REACT_APP_REDIRECT_URI=http://localhost:3000

# Optional: Specific tenant ID
REACT_APP_TENANT_ID=your-tenant-id-here

# Optional: Additional scopes
# REACT_APP_ADDITIONAL_SCOPES=https://graph.microsoft.com/Mail.Read,https://graph.microsoft.com/Calendars.Read

# Optional: For multi-tenant apps
REACT_APP_TENANT_TYPE=organizations

# Application URLs
REACT_APP_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# API Scopes for Azure Communication Services
REACT_APP_ACS_SCOPE=https://communication.azure.com/Chat

# Environment
REACT_APP_ENVIRONMENT=development

# Azure Communication Services Configuration
# Copy this file to .env.local for local development

# Azure Functions API Base URL
REACT_APP_AZURE_FUNCTIONS_BASE_URL=http://localhost:7181

# Teams Notification Configuration
# The API base URL is used for Teams notifications (uses same Azure Functions endpoint)
# REACT_APP_API_BASE_URL will default to REACT_APP_AZURE_FUNCTIONS_BASE_URL if not specified

# Optional: Direct ACS endpoint (fallback)
REACT_APP_ACS_ENDPOINT_URL=https://your-acs-resource.communication.azure.com

# Optional: Development mode settings
REACT_APP_ACS_DEBUG_MODE=true
REACT_APP_LOG_LEVEL=info

# Production settings (set these in your deployment environment)
# REACT_APP_AZURE_FUNCTIONS_BASE_URL=https://your-function-app.azurewebsites.net
# REACT_APP_ACS_DEBUG_MODE=false
